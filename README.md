### 部署   
```
# clone the project
git clone https://github.com/hardeasy/log-ui.git

# enter the project directory
cd log-ui

# install dependency
npm install

## 修改配置文件
打开.env.production 修改 VUE_APP_BASE_API为日志服务器地址  

# 发布
npm run build:prod

```
